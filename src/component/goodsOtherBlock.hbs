{{#with goodsOtherBlock}}
<section class="col-12">
    <h4 class="text-center mb-4">{{title}}</h4>
    <div class="row">
        {{#each cards}}
        <div class="col-4">
            {{> shopcard}}
        </div>
        {{/each}}
    </div>
</section>
{{/with}}